{
  "name": "gutenberg-examples",
  "version": "1.0.0",
  "source": [
    "src/index.jsx",
    "src/index.scss"
  ],
  "scripts": {
    "watch": "parcel --dist-dir build",
    "build": "parcel build --dist-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "parcel": "^2.6.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.0"
  }
}
